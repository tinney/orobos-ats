<div class="max-w-3xl">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">New Scorecard Template</h1>
    <p class="mt-1 text-sm text-gray-500">
      Creating template for <strong><%= @interview_phase.name %></strong> phase of <strong><%= @role.title %></strong>
    </p>
  </div>

  <%= render "form", template: @template %>
</div>
