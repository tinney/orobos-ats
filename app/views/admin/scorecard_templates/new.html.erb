<div class="max-w-3xl">
  <div class="mb-6">
    <%= link_to "← Back to Templates", admin_scorecard_templates_path, class: "text-sm text-indigo-600 hover:text-indigo-800" %>
    <h1 class="text-2xl font-bold text-gray-900 mt-2">New Scorecard Template</h1>
    <p class="mt-1 text-sm text-gray-500">
      Define rating categories that interviewers will use when evaluating candidates.
    </p>
  </div>

  <%= render "form", template: @template %>
</div>
