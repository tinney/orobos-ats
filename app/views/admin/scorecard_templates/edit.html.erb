<div class="max-w-3xl">
  <div class="mb-6">
    <%= link_to "← Back to Template", admin_scorecard_template_path(@template), class: "text-sm text-indigo-600 hover:text-indigo-800" %>
    <h1 class="text-2xl font-bold text-gray-900 mt-2">Edit Scorecard Template</h1>
    <p class="mt-1 text-sm text-gray-500">
      Editing "<%= @template.name %>"
    </p>
  </div>

  <%= render "form", template: @template %>
</div>
