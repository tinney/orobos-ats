<% content_for(:title, "Add Team Member — Admin") %>

<div class="w-full max-w-lg mx-auto">
  <div class="mb-6">
    <%= link_to "← Back to Team", admin_users_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>
  <h1 class="text-2xl font-bold text-gray-900 mb-1">Add Team Member</h1>
  <p class="text-sm text-gray-500 mb-6">Invite a new team member. They will receive a magic link to log in.</p>
  <%= render "form", user: @user %>
</div>
